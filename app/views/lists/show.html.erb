<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @list.title %>
</p>

<p>
  <strong>Due date:</strong>
  <%= @list.due_date %>
</p>

<p>
  <strong>Done:</strong>
  <%= @list.done %>
</p>

<%= link_to 'Edit', edit_list_path(@list), class: "btn btn-info"%> </td> |
<%= link_to 'Back', lists_path, class: "btn btn-primary"%>
